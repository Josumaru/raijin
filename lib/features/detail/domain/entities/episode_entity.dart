class EpisodeEntity {
  final String title, endpoint, date;
  final int episode;
  const EpisodeEntity({
    required this.title,
    required this.endpoint,
    required this.date,
    required this.episode,
  });
}
